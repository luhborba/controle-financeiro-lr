name: Deploy Produção

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    # Só roda se os testes passarem (proteção dupla)
    needs: []  # Pode adicionar job de testes aqui se quiser
    
    steps:
      - name: Checkout código
        uses: actions/checkout@v4
      
      - name: Deploy
        run: |
          echo "🚀 Deploy para produção autorizado"
          echo "Autor: ${{ github.actor }}"
          echo "Commit: ${{ github.sha }}"
          # Aqui vai o script de deploy real (Docker, SSH, etc)

